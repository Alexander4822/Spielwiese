# Required secret for HMAC session signing.
AUTH_SECRET=replace-with-long-random-secret

# Single-user password hash in format: scrypt:<saltHex>:<digestHex>
APP_PASSWORD_HASH=scrypt:replace-salt-hex:replace-digest-hex
